// const prompts = require('prompts');

// (async () => {
//   const response = await prompts({
//     type: 'text',
//     name: 'value',
//     message: 'Jak masz na imię?',
//     validate: (value : string) => value.length < 2 ? `Podaj imię` : true
//   });

//   console.log("Cześć " + response.value);
// })();

console.log("Hello World!!!")